# Default template — for split dev/prod DBs see:
#   .env.development.example  → copy to .env.development (local npm run dev)
#   .env.production.example   → production DB name on Render

MONGO_URI="mongodb+srv://USER:PASSWORD@cluster.mongodb.net/?appName=YourApp"
MONGO_DB_NAME="lumora_dev"
JWT_SECRET="change-me-to-a-long-random-secret"
PORT=5000
