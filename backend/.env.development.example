# Local development / testing database (copy to .env.development)
# Same Atlas cluster as production; different database name = isolated test data.

MONGO_URI="mongodb+srv://USER:PASSWORD@initialcluster.1koezfv.mongodb.net/?appName=InitialCluster"
MONGO_DB_NAME="lumora_dev"
JWT_SECRET="local-dev-secret-change-me"
PORT=5000
