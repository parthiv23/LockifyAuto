# Production database (copy to .env on Render or for local prod smoke tests)
# Render: set these in the dashboard Environment tab instead of a file.

MONGO_URI="mongodb+srv://USER:PASSWORD@initialcluster.1koezfv.mongodb.net/?appName=InitialCluster"
MONGO_DB_NAME="lumora"
JWT_SECRET="strong-random-production-secret"
PORT=5000
