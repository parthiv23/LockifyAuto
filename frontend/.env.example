# Backend URL when frontend is deployed separately (no trailing slash).
# Leave empty for local dev (Vite proxies /api to localhost:5000).
VITE_API_URL=

# Optional: override dev proxy target (default http://localhost:5000)
# VITE_API_PROXY_TARGET=http://localhost:5000
