{
  "name": "lockify-auto",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w lockify-backend\" \"npm run dev -w lockify-frontend\"",
    "dev:backend": "npm run dev -w lockify-backend",
    "dev:frontend": "npm run dev -w lockify-frontend",
    "build": "npm run build -w lockify-frontend && npm run build -w lockify-backend",
    "build:frontend": "npm run build -w lockify-frontend",
    "build:backend": "npm run build -w lockify-backend",
    "start:backend": "npm run start -w lockify-backend",
    "check": "npm run check -w lockify-frontend && npm run check -w lockify-backend",
    "db:push": "drizzle-kit push"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "drizzle-kit": "^0.30.4"
  }
}
